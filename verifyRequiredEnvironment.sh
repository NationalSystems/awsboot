
#
# TODO fix this, so we can verify that derived images have the right environment variables
#
#if [ "x$appname" = "x" ]; then echo "Need ENV appname defined" && exit 1; fi
#if [ "x$dnsdomain" = "x" ]; then echo "Need ENV dnsdomain defined" && exit 1; fi
